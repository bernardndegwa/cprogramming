#include <stdio.h>
int main(){

	int c,previousc;
	previousc = EOF;
while((c=getchar()) != EOF){
	if ((c == ' ') && (previousc == c) )
		previousc = c;
			putchar(c);
		}
	}
	if(c !=' '){
	putchar(c);
	}
}
 
